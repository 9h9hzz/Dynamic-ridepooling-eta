# Model boundary

The portfolio includes a self-contained tabular Transformer component. The full NODE training implementation is not copied here because the private workspace contains an adapted third-party source tree and manuscript-specific experiment code. Publish a NODE wrapper only after verifying the upstream license, attribution requirements, and ownership of local modifications.

